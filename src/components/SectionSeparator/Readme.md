A section separator built of two horizontal lines with diamonds on each side and a text between. The text can be passed via the <i>text</i> prop of the component.

```js
<SectionSeparator text="Your text" />
```